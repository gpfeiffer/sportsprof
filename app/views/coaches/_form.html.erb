<%= form_for(@coach) do |f| %>
  <% if @coach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coach.errors.count, "error") %> prohibited this coach from being saved:</h2>

      <ul>
      <% @coach.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <table>
    <tr>
      <td align="right">
	<%= f.label :first_name %>
      </td>
      <td>
	<%= f.text_field :first_name %>
      </td>
    </tr>
    <tr>
      <td align="right">
	<%= f.label :last_name %>
      </td>
      <td>
	<%= f.text_field :last_name %>
      </td>
    </tr>
    <tr>
      <td align="right">
	<%= f.label :gender %>
      </td>
      <td>
	<%= f.select :gender, Coach::GENDERS, :include_blank => true %>
      </td>
    </tr>
  </table>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
